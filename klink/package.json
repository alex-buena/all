{
	"name": "klink",
	"private": true,
	"scripts": {
		"dev": "bunx sst dev",
		"deploy": "bunx sst deploy",
		"check": "bunx --bun biome check",
		"check:fix": "bunx --bun biome check --fix",
		"dev:db": "docker compose --env-file .env up",
		"kit": "bunx sst shell -- drizzle-kit"
	},
	"workspaces": [
		"packages/*"
	],
	"dependencies": {
		"sst": "^3"
	},
	"devDependencies": {
		"@biomejs/biome": "2.2.3",
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}
